version: '2'
services:
  gesschool-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/GesSchool/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=GesSchool
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
